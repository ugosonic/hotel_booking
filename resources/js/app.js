import './bootstrap';
// resources/js/app.js

import VanillaCalendar from 'vanilla-calendar';

// Make it globally available (so inline scripts in Blade can call `new VanillaCalendar(...)`)
window.VanillaCalendar = VanillaCalendar;


